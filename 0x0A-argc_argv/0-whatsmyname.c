#include <stdio.h>

/**
 * main - Entry point
 *
 * @argc: Argument count
 * @argv: Argument vector
 * Return: int Always 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
		printf("%s\n", argv[0]);

		return (0);
}
